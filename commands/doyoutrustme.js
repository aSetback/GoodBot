module.exports = {
   if (message.author.username == "Memeoverlord"){
       message.channel.send("of course i dont trust you you fucking cuck");
       message.channel.send("who the fuck trusts Meme anyways");
       return;
     }
     if (message.isAdmin) {
      message.channel.send("I trust you");
    } else {message.channel.send("I do not trust you");};
  }
}
